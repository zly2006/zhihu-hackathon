# 决策路径语料库

本上下文描述如何把公开叙事中的人生决策经历组织成可检索、可比较的情景、路径与分叉。所有内容都表示“来源所陈述的经历或观点”，不自动等同于经验证事实或因果结论。

**决策知识库（Decision Knowledge Base）**：由通过自动质量门的决策经历、规范情景、叙事路径和分叉路口组成，并且每个正式字段都能回链到来源快照中的证据片段。原始回答、抽取候选和搜索结果是知识库的来源与候选层，不等于正式知识。
_避免：回答列表、推荐系统、人生标准答案_

## 来源与证据

**来源内容（Source Content）**：
从外部来源取得的一篇完整内容，例如一条知乎回答；它是采集与版权边界，不是最小分析单元。
_避免：单条数据、决策样本_

**内容快照（Content Snapshot）**：
来源内容在某次合法获取时的版本，用于区分编辑、删除和互动计数变化。
_避免：当前正文、原始行_

**证据片段（Evidence Span）**：
内容快照中直接支持某个抽取字段或关系的一段可定位文本；没有证据片段的模型结论不得成为已确认数据。
_避免：引用（未带定位时）、模型理由_

**知识片段（Knowledge Fragment）**：
从内容快照中抽出的、带原文位置和内容分型的一小段语义材料；它可以是亲历、建议、比较、转述、假设或推广，不要求同时包含完整决策经历。
_避免：把每条回答强制做成决策卡_

**叙事主张（Narrative Claim）**：
来源内容声称成立的命题，并明确区分亲历、自述结果、转述、建议和反事实设想。
_避免：事实、真相_

**语义处理任务（Semantic Processing Task）**：
由数据库排队、带输入快照和版本信息的一次有限语义处理工作，例如提取片段、组合决策经历或提出情景归属；任务可重试、可取消、可幂等。
_避免：让模型自由浏览全库、一次性改写正式知识_

**语义提案（Semantic Proposal）**：
模型针对处理任务输出的结构化候选，必须带输入 hash、提示词/模型版本和证据片段；通过确定性校验后自动成为正式数据，未通过的自动排除并保留原因。
_避免：模型输出、自动事实_

**默认语义模型（Default Semantic Model）**：
当前用于 L1 批量语义处理的 `GLM-4.7-Flash`；它只产生带证据的语义提案，不直接写入正式知识。限流、配额耗尽或低置信度时，任务按升级策略切换到备用模型。
_避免：把免费模型当成无限吞吐或事实裁决者_

**决策情景检索（Decision Scenario Retrieval）**：
给定当前背景、决策点和关键约束，召回并排序相似的 `CanonicalScenario`、分叉和结果证据；它是回答生成前的证据层，不负责决定答案。
_避免：把检索相似度当成推荐或成功概率_

**证据约束回答（Evidence-grounded Answer）**：
应用层基于已自动确认的决策情景、分叉、结果观察和原文证据生成给用户的说明；回答不是新的事实，不得写回知识库，缺少可比证据时必须明确说明。
_避免：脱离检索证据自由发挥、把回答写回事实表_

## 决策语义

**决策经历（Decision Episode）**：
可选的事件级分析单元：某个主体在一种前置状态下，面对一个决策点，采取或考虑某项行动，并可能陈述后续状态或结果。一篇来源内容可产生零个、一个或多个决策经历；无法证明事件边界时停留在知识片段层。
_避免：回答、单条回答、故事_

**前置状态（Situation State）**：
决策发生前、与选择有关的情境与约束的时间截面；未知信息必须保持未知，不能由模型补齐。
_避免：用户画像、标签包_

**决策点（Decision Point）**：
前置状态中需要在两个或多个可行动方向之间作出选择的时刻或问题。
_避免：问题（容易与知乎问题混淆）、节点_

**决策情景（Decision Scene）**：
一条来源叙事中具体发生选择的中段，至少包含决策点和可行动方向；如果来源明确，还包含实际选择、行动和权衡理由。它是 `DecisionEpisode` 的事件内容，不等于跨回答归并的规范情景。
_避免：把回答标题、主题标签或 `CanonicalScenario` 直接当成这次选择_

**行动类型（Action Archetype）**：
将语义等价的具体行动归一后的类别，例如“继续读研”；它只合并行动含义，不合并前置状态或结果。
_避免：选项、标签_

**结果观察（Outcome Observation）**：
来源在某个时间跨度后陈述的后续状态或评价，必须保留观察窗口和主张类型；它不是对行动的因果归因。
_避免：效果、收益、因果结果_

**拟议路径（Proposed Path）**：
来源提出“应该如何做”的方向，但没有证明作者或被叙述主体实际采取并观察到后果；它可以作为建议证据或待验证选项，不能与已发生的决策经历共用结果统计。
_避免：把建议当成亲历、把观点当成结果_

## 聚合与路径

**规范情景（Canonical Scenario）**：
多个决策经历共享的一组可比较前置状态与同类决策点；情景只描述“从哪里出发”，不预设哪种行动更好。
_避免：主题、问题分类、用户群_

**叙事路径（Narrative Path）**：
单个可自洽来源叙事中，由时间和证据连接的“状态—行动—后续状态”有序序列；默认不跨作者拼接个人经历。
_避免：人生轨迹、完整人生_

**路径模式（Path Pattern）**：
由多条叙事路径归纳出的规范序列，表达常见走向及其证据覆盖，不代表个体预测。
_避免：标准答案、成功路线_

**分叉路口（Branch Point）**：
一个规范情景，在证据中连接到至少两种不同的行动类型，并且各行动具有可追踪的后续路径或结果观察；它表示可比较选择，不表示推荐或因果优劣。
_避免：热门选项、观点冲突、最佳选择_

**候选分叉（Branch Candidate）**：
尚未满足证据数量、可比性或自动质量门槛的潜在分叉路口；它会被保留为 `REJECTED` 或下一版本的候选，不进入用户端。
_避免：把自动排除记录当成正式分叉_
