# Agent 工作边界

## 一句话

本项目是**面向决策对象的案例检索库**：先识别用户“正在决定什么”，再从同一决策对象下检索真实知乎案例、选择、结果和原文证据。

## 核心闭环

```text
知乎问题标题 → 决策对象 → 可选 COEL 行为锚点
知乎回答     → 该对象下的案例与原文证据

用户输入 → 识别决策对象 → 进入同一对象 → 按完整情景排序案例
        → 返回选择、结果、知乎 URL 与 HTML 证据
```

## 归类原则

- `DecisionObject` 只回答“用户正在决定什么”，不包含背景、原因和结果。
- 规范说法相同才归入同一决策对象；COEL 只提供上位语义锚点，不能单独决定合并。
- 背景、目标和约束不创建新分类，只用于同一对象内的案例排序。
- 实际选择和结果不参与对象归类，只作为决策证据。
- 问题标题优先提供决策对象；回答正文提供案例内容，不能用回答中的泛泛建议替代问题本身。

## 数据底线

- 原始知乎 URL、HTML、正文和快照必须保留并可回链。
- 决策对象提取必须保存模型版本、COEL 版本、原始问题标题和置信度。
- 模型只能提出结构化对象；对象格式、置信度或批次质量门不通过时不得写库。
- COEL 只能由本地高精度别名表挂接，模型不得自由猜编码。
- embedding 只负责匹配用户查询和在对象内排序案例，不能直接生成推荐或因果结论。

## KISS 判断标准

新增功能只需要回答三个问题：

1. 它是否让“用户正在决定什么”识别得更准？
2. 它是否让同一决策对象下的真实案例更容易检索？
3. 它是否保留知乎原文证据和处理版本？

否则不属于当前核心闭环。
