# Agent 工作边界

## 一句话

本项目的核心是**决策情景检索系统**：从知乎回答中整理出可比较的决策情景，匹配相似情景，并把分叉、结果和原文证据交给应用层生成回答。

## 核心闭环

```text
知乎回答
  → 决策事件
  → 规范情景
  → 分叉路口
  → 情景向量
  → 相似情景检索
  → 分叉、结果、原文证据
  → 证据约束回答（应用层）
```

## 两条处理链

### 离线：建设知识库

- 从回答中抽取“决策背景—决策情景—决策结果”。
- 将可比较的决策事件合并为 `CanonicalScenario`。
- 将同一情景下不同实际行动整理为 `BranchPoint` / 分叉。
- 为规范情景生成 embedding，供检索使用。
- 模型只能产生带证据的结构化候选，不能直接写入正式事实。

### 在线：先检索，再回答

```text
当前背景 + 决策点 + 关键约束
  → 查询向量
  → 向量召回 + 结构化过滤
  → 相似规范情景
  → 分叉、结果观察、URL/HTML 证据
  → 基于证据生成回答
```

在线回答只能使用检索到的已审核证据；不能把相似度当成“你应该选什么”，不能计算成功概率，也不能把相关性写成因果结论。回答不写回知识库。

## 数据底线

- 原始知乎 URL、HTML 和快照必须保留并可回链。
- 一个回答可以产生 0..N 个决策事件，也可以没有语义事件。
- 每个正式字段都必须能定位到原文证据片段。
- 建议、转述和假设不能伪装成亲历事件。
- 未审核的候选不能参与正式情景和分叉检索。
- 检索结果必须返回匹配依据、数据版本和证据 ID。

## 模型边界

GLM、DeepSeek 或其他模型只用于离线语义处理：分类、抽取、归一化、合并候选和生成情景文本。模型可替换，数据库契约、证据校验和审核流程不可被模型绕过。

embedding 负责召回候选；向量相似度不能单独决定情景合并，仍需结构化字段、证据和审核共同确认。

## KISS 判断标准

新增功能前先问：

1. 它是否让“相似决策情景检索”更准确或更可追溯？
2. 它是否保留原文证据和数据版本？
3. 它是否避免把系统扩张成脱离证据的聊天、推荐或人生答案产品？

如果不能回答“是”，就不属于当前核心闭环。
