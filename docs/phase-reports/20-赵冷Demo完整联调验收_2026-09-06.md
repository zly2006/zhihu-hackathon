# 赵冷 Demo 完整联调验收（2026-09-06）

## 范围确认

- D1：12 节拍、33 个选择反馈、三个结局已确认。
- D2：固定剧本作为稳定验收路径；AI 写作只生成公开对白/旁白，规则、跳转、数值结算由程序负责。
- D3：使用现有场景运行时、选择提交、存档、快照和叙事体验链路；不新增数据库迁移、同步或后台循环。
- 按用户要求直接在 `feature/scene-runtime` 当前工作区开发，未创建隔离分支、未新建 worktree、未推送或合并。

## 已交付

- 十二节拍剧本：`zl-01-message` 至 `zl-12-hook`。
- 11 个正式选择节拍共 33 个 A/B/C 选择反馈。
- 普通结局：`mutual-trust`、`kind-distance`；隐藏结局：`library-letter`。
- 借阅卡观察、一次性隐藏来信消费、结局幂等回执。
- 固定剧本 / AI 写作双模式；AI 输出只允许公开叙事字段，禁止写入世界状态、规则、关系数值和 NPC 私密状态。
- 固定剧本与 AI 模式独立存档键，场景位置、选择、快照和叙事运行时均可序列化。
- `/life/zhao-leng` 页面及 `/api/life/zhao-leng`、`/command`、`/experience` 接口。

## 验证证据

### 自动化

- `node scripts/test-all.mjs`：`ALL TEST SUITES PASS`。
- 赵冷专项覆盖：12 节拍、33 反馈、三结局、完整固定路线、关系分歧、隐藏结局一次性消费、专用规则越界拦截、存档往返、AI writer 私密字段拦截与重试。
- `npm run lint`：退出码 0。
- `npm run build`：退出码 0；仅保留既有 `lib/narrative/query-adapter.ts` 动态文件系统追踪警告。

### 真实运行实例 SSE

- `POST http://127.0.0.1:3000/api/event`：HTTP 200，`text/event-stream`。
- 收到 34 个 `progress` 事件，无 `error` 事件。
- 最终 `complete`：召回经历数 `18`，选项数 `3`，年龄 `24`，年份 `2019`。

### 浏览器联调

- Edge 真实页面完成多次选择提交，反馈文本和关系数值更新可见。
- 时间线最终显示 `12 / 12 节拍完成`。
- 隐藏来信入口可打开，信件可读完，结局收束界面显示 `结局：library-letter`。
- 当前 Codex 浏览器扩展环境在页面评估上下文中不提供 `localStorage`，因此无法用该扩展做刷新后的浏览器存档续接观测；存档键、序列化/反序列化、选择提交后保留和分支隔离均由专项自动化测试覆盖。正常浏览器运行时仍使用固定剧本 / AI 独立 `localStorage` 键。

## 提交边界

本次提交只包含赵冷 Demo 的运行时、叙事、接口、页面、测试、测试注册和本报告。以下已有用户内容保持未提交：`.env`、`.zcode/`、用户提供的方案文档及 `docs/phase-reports/19-*` 等。
