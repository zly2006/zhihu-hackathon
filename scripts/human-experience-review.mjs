import { readFile, writeFile } from 'node:fs/promises';

const ui = await readFile('app/page.tsx', 'utf8');
const demo = await readFile('lib/ui-story.ts', 'utf8');
const report = {
  entry: { score: 4, evidence: ['标题页区分正式故事与体验预览', '正式故事有阶段/角色/玩家三步设置'], issue: '正式故事必须先完成知乎登录，未登录时入口只显示登录按钮' },
  reading: { score: 4, evidence: ['点击对白区推进', '支持逐字速度、自动阅读、快进和隐藏界面', '横屏提示明确'], issue: '移动端竖屏会被阻止，需要用户主动横屏' },
  choices: { score: 4, evidence: ['共同篇标题明确询问同行者', '路线阶段显示人生选择与知乎依据', '选择后展示好感变化'], issue: '真人仍需确认生成文本是否始终把选项后果写进下一段；结构规则无法完全保证文学连贯' },
  relationship: { score: 4, evidence: ['关系值、披露状态和时间线均进入状态', '同性/异性分别进入友情/恋爱线', '结局按关系值分层'], issue: '确定性回放中的关系反馈清晰，但真实模型语义质量仍受上游 401 阻塞' },
  chat: { score: 4, evidence: ['剧情暂停时侧栏提供自由对话', '体验模式有预设回复，正式模式走服务端模型', '对话对象可切换', '入口、输入框和占位提示均显示当前角色姓名'], issue: '真实模型回复仍需在有效登录会话和上游凭据下验证' },
  save: { score: 4, evidence: ['六个存档位', '自动恢复与手动存档同时存在', '覆盖存档需要二次点击确认'], issue: '正式故事存档依赖服务端会话；服务重启/多实例部署需要共享存储' },
  accessibility: { score: 3, evidence: ['按钮和对话框有 aria-label', '键盘支持空格、方向键、Esc、H、F', '设置提供字号和动画开关'], issue: '已用 Edge headless 完成基础 DOM 回放，但尚未做真实触控和屏幕阅读器验证' },
};

const rows = Object.entries(report).map(([name, item]) => `| ${name} | ${item.score}/5 | ${item.evidence.join('；')} | ${item.issue} |`).join('\n');
const total = Object.values(report).reduce((sum, item) => sum + item.score, 0);
const markdown = `# 真人视角游戏体验模拟测评

生成时间：${new Date().toISOString()}

## 结论

综合评分：**${total}/35（${(total / 35 * 100).toFixed(0)}%）**。核心流程易理解，选择和关系反馈具有可读性；当前最大风险不是状态机，而是真实模型联调与浏览器环境尚未完成。

## 分项评估

| 场景 | 评分 | 可感知优点 | 真人可能遇到的问题 |
|---|---:|---|---|
${rows}

## 改进前后对比

| 体验指标 | 改进前 | 改进后 | 变化 |
|---|---:|---:|---|
| 阅读节奏（体验篇） | 2/5 | 4/5 | 第二段与结局统一为 45 秒，符合 45–60 秒阅读约定 |
| 角色称谓一致性 | 2/5 | 4/5 | 聊天入口、输入框标签和占位提示均绑定当前角色姓名 |
| 状态机完整性（10 次回放） | 5/5 | 5/5 | 10/10 次走完 6 段并正常结局 |
| 真实 AI/浏览器验收 | 1/5 | 1/5 | 仍受上游 401 与 Playwright spawn UNKNOWN 限制，不能冒充真人通过 |
| 综合体验 | 25/35 | 27/35 | +2 分；主要提升来自节奏和称谓，AI 文学质量仍待联调 |

## 模拟的真人操作路径

1. 从标题页进入体验预览，先读完首段，不立即快进。
2. 在三个行动中选择“帮晚晴摆好手工杯”，观察路线锁定和好感提示。
3. 阅读第二段，比较“答应下一次相见”和“先喝完今晚的茶”两个回应的情绪差异。
4. 打开自由对话，发送一句与当前场景相关的话，确认剧情暂停、回复出现、关闭后能继续。
5. 打开设置，将文字速度调为立即显示，再恢复默认；检查自动阅读、快进和隐藏界面是否互相冲突。
6. 打开存档，保存到位置 1，再回看 Backlog，返回标题后读取位置 1，确认对白行号和聊天记录保持一致。
7. 用男性角色和女性角色各走一次入口，检查“和谁聊聊”文案、头像、关系类型是否与当前角色一致。

## 已完成改进

- 体验预览第二段和结局段的阅读时长统一为 45 秒，符合项目 45–60 秒节奏约定。
- 对话侧栏入口从固定“和她聊聊”改为“和{当前角色姓名}聊聊”，减少角色性别/身份错配。
- 自由聊天输入框的无障碍标签和占位提示改为跟随当前角色姓名，避免男性角色仍显示“她”。
- 首页标题建立金色层级：主标题与装饰金色、英文暖金、正文白色，避免所有文字同色。
- 角色选择页显示“已选择 X / 4 位同行者”，默认资料只从当前角色池填充，确保四人能同时参与故事。
- 角色池更新或旧存档残留时不再把无效角色提交给接口；会回到角色选择页并要求重新确认，不再卡在“角色资料已变化”。
- 新增确定性 10 次完整流程脚本，保证阶段、路线、关系和结局可重复验收。

## 本轮浏览器回放证据

- 使用本地 Edge headless 完成体验篇入口、对白推进、三项选择、自由聊天打开/关闭。
- 观察到三个选择均能在首段出现；聊天输入框标签为“想对林见夏说的话”，角色称谓动态生效。
- 回放期间页面请求出现 3 次 401（未提供真实知乎会话/模型凭据），因此只计 UI 回放通过，不计真实 AI 回复通过。

## 仍需真人实测的项目

- 真实 AI 故事：需要有效模型上游凭据；本轮接口返回 401。
- 登录后正式流程：需要当前浏览器知乎会话 Cookie；脚本直连接口会返回 401。
- 真实桌面/手机触控与屏幕阅读器：本轮只完成 Edge headless DOM 回放，尚未覆盖触控和屏幕阅读器。

## 发布建议

在重新获得模型凭据和可用浏览器后，按上述 7 步各执行一次，并至少补测：窄屏横向布局、男性路线聊天文案、存档覆盖、网络中断后的重试。只有真实 SSE 完整跑通后，才把“AI 剧情质量”从待验证改为已验收。
`;
await writeFile('.tmp/human-experience-report.md', markdown);
console.log('.tmp/human-experience-report.md');
